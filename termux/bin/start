#!/data/data/com.termux/files/usr/bin/sh

termux-wake-lock
sshd

# sleep 5s
# sudo setprop service.adb.tcp.port 5555
# sudo stop adbd
# sudo start adbd

# sudo am start -S com.android.settings/.TetherSettings && sudo input keyevent 20 && sudo input keyevent 20 && sudo input keyevent 66
